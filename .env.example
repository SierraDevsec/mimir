# Mimir Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# ── Mimir Daemon ──────────────────────────────────────
# MIMIR_PORT=3100

# ── Cloudflare Workers AI (RAG embeddings) ────────────
# Required for semantic search. Without these, Mimir falls
# back to text-based ILIKE search (still works, less precise).
# Model: @cf/baai/bge-m3 (free tier, 1024-dim, multilingual)
# Get yours: https://dash.cloudflare.com → AI → Workers AI
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=

# ── API Authentication (optional) ─────────────────────
# Set this to require Bearer token auth on all API/hook endpoints.
# Useful when exposing the daemon via SSH tunnel or container port mapping.
# All consumers (hook.sh, MCP server, VSCode Extension, Web UI) read this automatically.
# MIMIR_API_TOKEN=your-secret-token-here

# ── Slack Integration (optional) ──────────────────────
# SLACK_BOT_TOKEN=xoxb-...
# SLACK_APP_TOKEN=xapp-...
# SLACK_CHANNEL_ID=C...
# SLACK_PROJECT_ID=my-project
